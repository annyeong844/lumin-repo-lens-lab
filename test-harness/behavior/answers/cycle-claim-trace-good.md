I checked the dependency graph before making the cycle claim.

Scan range: 42 TS/JS files. The important signal is one 4-file cycle, so the next step is to inspect that strongly connected component and split only one edge.
